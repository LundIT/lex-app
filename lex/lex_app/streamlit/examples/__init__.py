"""Reference dashboards, meant to be copied rather than imported.

``_streamlit_structure`` is the file a project copies to its own repo root. It
lives here so it ships with the framework and can be imported by a test -- an
example nobody runs is an example that rots.
"""
