cluster: 1
slug: init
title: Init — Project Bootstrap
max_scenario: 302
letters:
  a:
    title: '`lex setup` — scaffolding'
    scenarios: 1.1-1.5
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: counts folded into cluster top-line in pre-migration dashboard; per-letter tally not separately
      recorded
  b:
    title: '`lex Init` — first-run initialization'
    scenarios: 1.6-1.16
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: counts folded into cluster top-line in pre-migration dashboard; per-letter tally not separately
      recorded
  c:
    title: '`INITIAL_DATA` loading (part of `lex Init`)'
    scenarios: ''
    status: planned
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: counts folded into cluster top-line in pre-migration dashboard; per-letter tally not separately
      recorded
  d:
    title: ''
    scenarios: 1.23-1.30
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: counts folded into cluster top-line in pre-migration dashboard; per-letter tally not separately
      recorded
  e:
    title: ''
    scenarios: 1.30-1.43
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: counts folded into cluster top-line in pre-migration dashboard; per-letter tally not separately
      recorded
  f:
    title: ''
    scenarios: 1.8-1.15
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: counts folded into cluster top-line in pre-migration dashboard; per-letter tally not separately
      recorded
  g:
    title: ''
    scenarios: 1.44-1.46
    status: complete
    tests:
      pass: 10
      skip: 0
      xfail: 0
    note: '`_make_sync_manager()` fixture, no Keycloak/DB'
  h:
    title: ''
    scenarios: 1.47-1.50
    status: complete
    tests:
      pass: 15
      skip: 0
      xfail: 0
    note: '`requests.get` patched, network-free'
  j:
    title: ''
    scenarios: ''
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: on disk; per-letter tally folded into cluster top-line in pre-migration dashboard
  k:
    title: ''
    scenarios: ''
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: on disk; per-letter tally folded into cluster top-line in pre-migration dashboard
  l:
    title: ''
    scenarios: ''
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: on disk; per-letter tally folded into cluster top-line in pre-migration dashboard
  m:
    title: ''
    scenarios: ''
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: on disk; per-letter tally folded into cluster top-line in pre-migration dashboard
  n:
    title: ''
    scenarios: ''
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: on disk; per-letter tally folded into cluster top-line in pre-migration dashboard
  o:
    title: Lazy imports + sync-exclusion + history-config helpers
    scenarios: ''
    status: complete
    tests:
      pass: 15
      skip: 0
      xfail: 0
    note: scenarios 1.110–1.124
  p:
    title: Settings / config / URLs / top-level views
    scenarios: ''
    status: complete
    tests:
      pass: 22
      skip: 0
      xfail: 0
    note: scenarios 1.125–1.146
  q:
    title: Migration file completeness gate
    scenarios: '1.147'
    status: complete
    tests:
      pass: 1
      skip: 0
      xfail: 0
    note: scenario 1.147; `lex makemigrations ... --check --dry-run` must stay clean for framework apps
  s:
    title: Log-noise cleanup + lex-namespace debug control (EXC-1787)
    scenarios: ''
    status: complete
    tests:
      pass: 10
      skip: 0
      xfail: 0
    note: scenarios 1.159–1.168; urllib3 InsecureRequestWarning gate + `LEX_LOG_LEVEL` lex-only DEBUG
      + console-handler level + blanket `LEX_SUPPRESS_WARNINGS` filter
  t:
    title: '`DISABLE_SERVER_SIDE_CURSORS` placement (production cursor crash)'
    scenarios: 1.169-1.170
    status: complete
    tests:
      pass: 2
      skip: 0
      xfail: 0
    note: scenarios 1.169–1.170; flag was module-level (ignored by Django) so server-side cursors stayed
      on behind the pooling proxy → `InvalidCursorName` on every `.iterator()`; now set per
  u:
    title: Fast ASGI health/readiness probes
    scenarios: 1.171-1.175
    status: complete
    tests:
      pass: 5
      skip: 0
      xfail: 0
    note: scenarios 1.171–1.175; `/health` short-circuits to static liveness, `/readiness` gates on DB
      readiness, non-probe HTTP falls through to Django
  v:
    title: '`TIME_ZONE`↔`USE_TZ` coupling — `django_celery_beat` DatabaseScheduler correctness'
    scenarios: ''
    status: complete
    tests:
      pass: 5
      skip: 0
      xfail: 0
    note: scenarios 1.179–1.183; `TIME_ZONE="UTC"` when `USE_TZ=False` so beat's naive-as-UTC read is
      correct for the recovery `IntervalSchedule` AND future-edit `ClockedSchedule` (was off b
  w:
    title: '`LEX_TASK_RECOVERY_ENABLED` defaults OFF — stuck calc resets on restart'
    scenarios: 1.184-1.186
    status: complete
    tests:
      pass: 3
      skip: 0
      xfail: 0
    note: scenarios 1.184–1.186; default flipped `true`→`false` so the startup sweep blind-aborts a stuck
      `IN_PROGRESS` row on restart when no recovery-supervisor pod runs (local/CI/un-provi
  x:
    title: Health exposes encrypted runtime metadata for the Instance Controller
    scenarios: 1.187-1.194
    status: complete
    tests:
      pass: 0
      skip: 0
      xfail: 0
    note: 'scenarios 1.187–1.194 (renumbered from 1.171–1.178 during the 2026-07-07 BUG-023 letter-collision
      fix — this file previously shared letter 1u + IDs 1.171–1.175 with test_1u_fast_health_asgi.py;
      moved to fresh letter x + fresh IDs). Encrypted runtime version/SHA in the health payload for IC.'
  y:
    title: IDE-aware setup run configurations for PyCharm and VS Code
    scenarios: 1.203-1.210
    status: complete
    tests:
      pass: 8
      skip: 0
      xfail: 0
    note: best-effort IDE marker detection; unknown/conflicting environments generate both formats;
      VS Code launchers retain full PyCharm command/env/prompt parity and preserve user JSONC entries
  i:
    title: rebase_incident_datetimes — re-anchor datetimes corrupted by the TIME_ZONE incident
    scenarios: 1.195-1.200
    status: complete
    tests:
      pass: 6
      skip: 0
      xfail: 0
    note: management command that surgically re-anchors user-entered datetimes mis-stored after the
      rc212 TIME_ZONE flip (f622c9c); dry-run by default, PER-INSTANCE window [--cutoff, --until)
      (--cutoff required, no global default — too-early corrupts good data; --until defaults to now),
      excludes app-stamped created_at/edited_at. Correction is DST-aware (AT TIME ZONE per value's own
      date — winter −1h vs summer −2h); the ~2 transition hours/year are flagged for review. 1.197
      late-upgrader safety, 1.198 post-fix bound, 1.199 DST-aware winter shift, 1.200 transition-flag.
      Ships with the USE_TZ=True cutover.
  r:
    title: Fetched datetimes come back in the DB-session display zone (Berlin)
    scenarios: 1.201-1.202
    status: complete
    tests:
      pass: 2
      skip: 0
      xfail: 0
    note: DATABASES['default']['TIME_ZONE'] = TIME_ZONE runs the Postgres session in Berlin, so a
      fetched DateTimeField returns aware in the display zone (11:00+02:00) not UTC — str()/.date()/.hour
      and model __str__ render local with no per-field/per-model changes; storage + instant unchanged.
      Postgres-only guard; Django date-part lookups use explicit AT TIME ZONE so bitemporal/as_of raw
      SQL is unaffected (history+serializers+init+exports 385 pass with it live).
  z:
    title: Streamlit auth proxy - iframe re-auth breakout (refused-to-connect fix)
    scenarios: 1.211-1.216
    status: complete
    tests:
      pass: 6
      skip: 0
      xfail: 0
    note: >-
      unauthenticated iframe/frame document loads break out to a top-level login instead of
      redirecting the frame to Keycloak, whose login page sets frame-ancestors 'self' and renders
      as "refused to connect"; top-level redirect and API 401 unchanged. Covers lex/proxy.py
      _unauthenticated_response / _is_iframe_document_request. Renumbered from y/1.195-1.200 on
      merge - both the letter and the range were taken on lex-app-v2 while this was open
  aa:
    title: Embedded Streamlit token renewal (issuer expiry + proxy adoption)
    scenarios: 1.217-1.222
    status: complete
    tests:
      pass: 6
      skip: 0
      xfail: 0
    note: >-
      follow-up to the iframe breakout (batch z) - makes the expiry dead end avoidable rather than
      merely graceful. StreamlitTokenView now publishes expires_in/expires_at/refresh_interval
      (previously only returned by dead code that self-signed HS256, which the RS256/JWKS proxy
      would reject) and 401s instead of KeyError-ing when the session has no OIDC token; the proxy
      adopts a strictly newer auth_token rather than discarding it while the stored one is still
      valid, which silently defeated any renewal. First two-letter batch id - cluster 1 exhausted
      a-z. Renumbered from z/1.201-1.206 on merge
  ab:
    title: Ignored client-role self-cleanup (client-admin platform role, LEX-5)
    scenarios: 1.223-1.229
    status: complete
    tests:
      pass: 7
      skip: 0
      xfail: 0
    note: >-
      IGNORED_CLIENT_ROLES swaps the abandoned release-manager for the platform-internal
      client-admin role (init.py:49); the sync only ever ADDS Policy - <role> entries via
      Keycloak's import endpoint, so a policy minted by an older lex-app for a role that later
      became ignored would persist forever without an explicit cleanup. Adds
      strip_ignored_role_policies (in-memory: drops the ignored-role policy from auth_config and
      detaches its name from every permission's config.applyPolicies) and
      delete_stale_ignored_role_policies (live: finds Policy - <ignored role> via
      get_client_authz_policies and deletes it via delete_client_authz_policy once nothing
      references it, run after import_authorization_settings succeeds so Keycloak's
      referential-integrity check on the policy delete passes). 1.223 ignore-set contents, 1.224
      stale policy removed from config, 1.225 reference detached from a permission's
      applyPolicies, 1.226 no-op when nothing stale, 1.227 live delete by id, 1.228 no-op when
      nothing live, 1.229 missing-id fail-fast.

  ac:
    title: Streamlit session survival across an idle period (frozen-handshake fix)
    scenarios: 1.230-1.246
    status: complete
    tests:
      pass: 17
      skip: 0
      xfail: 0
    note: >-
      the third and last batch on this auth path (z made expiry graceful, aa let the embedded
      caller renew, ac stops the session expiring at all). Root cause: Streamlit reads request
      headers off the *websocket handshake*, so every credential the proxy injects is a snapshot
      that ages while the socket stays open - and session auth had local refresh deliberately
      disabled, leaving nothing to renew it. Adds proxy /auth/token (secret-guarded pull, proxy
      stays sole refresh authority so nothing races the rotation), reorders credential precedence
      so the renewable session outranks the short-lived st_access cookie on both the HTTP and WS
      paths, and on the Streamlit side stops the frozen header token overwriting a renewed one,
      renews ahead of expiry for session auth too, and decouples identity from token freshness -
      the "Missing user information" screen was reporting an expired token as a missing user, and
      re-entered itself on every rerun. 14 of 17 fail against the pre-fix tree.
  ad:
    title: Streamlit asset bundle served ungated + session durability guards
    scenarios: 1.247-1.302
    status: complete
    tests:
      pass: 56
      skip: 0
      xfail: 0
    note: >-
      the fourth batch on this auth path, and the one that explains the other three reports. The
      proxy authenticated every request through one catch-all route, and Streamlit 1.61 ships 365
      code-split JS chunks with 107 named in eager modulepreload tags - so one credential-less
      moment produced a 401 flood, and a lazily-imported chunk that 401s reaches the browser as
      "TypeError: Failed to fetch dynamically imported module", which is how an expired credential
      was reaching customers as a type error. Serves Streamlit's own bundle from the proxy process
      (located via streamlit.file_util.get_static_dir so hashes cannot drift from the index.html
      naming them), makes only /_stcore/health and /_stcore/host-config publicly proxied, and keeps
      everything else gated - the boundary scenarios matter as much as the public ones. Also: one
      pooled httpx client instead of one per request; raw streaming that preserves Content-Encoding
      (measured 1.77 MB -> 0.42 MB on the eager set, 4.17x); the JWKS fetch moved off the event
      loop with single-flight and stale-key retention (a failed refresh used to reject every token
      in the cluster); ?next= carried through login->callback with a strict open-redirect guard;
      the bootstrap auth_token stripped out of the URL; and startup refusal for the configurations
      that are genuinely broken rather than degraded - replicas without a shared token store,
      SameSite=None without Secure. The session key is DERIVED from DJANGO_SECRET_KEY (stable in
      terraform state, identical on every replica) rather than demanded as a new variable: an
      earlier revision refused to boot without SESSION_SECRET and bricked a running instance,
      which is a worse failure than the fragility it guarded against. 1.293-1.295 fix a regression the pooling change introduced and production found: Streamlit sits behind uvicorn, whose keep-alive timeout is 5s, so a pooled connection handed out after that fails with RemoteProtocolError before a byte is exchanged - observed in a prod log exactly 5s after the previous request, on a /media fetch, i.e. a document download that simply failed. The per-request client this replaced could never hit it. Pool expiry now undercuts the upstream's, one retry covers the race, and the error class answers 502 instead of escaping as an unhandled ASGI exception. 27 of 30 fail against the pre-fix tree; the 3 that pass
      (1.254 boundary, 1.255 WS deny, 1.271 permitted configs still boot) are guards asserting what
      the change must not break. Recorded BUG-029 (pre-existing cluster-1 flake) while running it. Scenarios 1.277-1.286 were added after an adversarial review of the first pass found ten further defects in it: the auth_token strip handed over fewer credentials than the response it replaced (no st_access, fatal wherever the frame cannot use the session cookie); JWKS backed off only when keys were already cached, so a cold cache cost every queued request its own 10s fetch; a bodiless 304 yielded an empty chunk that made GZip attach a body to it; a mid-body upstream failure became a truncated 200, i.e. a silent SyntaxError in a Vite chunk; the pooled client and the module-level locks were reusable across event loops; public_proxy forwarded a Content-Length it was not sending; the /static mount ignored baseUrlPath, silently restoring the 401 storm; a non-numeric LEX_PROXY_REPLICAS raised at import; the lifespan JWKS warmup delayed the port bind by up to 10s; and the CLI pre-check mirrored two of five import-time rules with a case-sensitive https test. Scenarios 1.287-1.290 then close the gap those fixes exposed: freezing the iframe src stops a renewal destroying the Streamlit session, but it also meant the renewed token had no way to REACH the proxy, since ?auth_token= in the iframe URL was the only channel that existed - so an embedded session still died at the access token's own lifetime with the shell holding a fresh token it could not deliver. POST /auth/adopt is that channel: token in the body rather than a URL, CORS-scoped to the configured frontend origin, JWKS-validated like every other credential, and adopted through the strictly-newer logic PR 723 already built for exactly this. The CORS allowlist is DERIVED from DOMAIN_HOSTED (1.291-1.292) rather than declared in a new variable: settings.py already refuses to start without it whenever DEPLOYMENT_ENVIRONMENT is set and Django builds CORS_ORIGIN_WHITELIST from it the same way, so it is guaranteed present - and a new required variable would have made the failure mode a dashboard that renews for five minutes and then quietly stops.
