**No user-facing changes in this release.** It contains developer and AI-tooling work only.

## Internal

- **Run configurations for VS Code.** The project generator produces VS Code run configurations alongside the existing PyCharm ones, so a freshly set-up project is runnable in either editor without hand-wiring.
- **`lex ai-*` tooling consolidated.** The AI assistant commands, their dashboard and their FAQ moved into the framework's tooling directory, with a reporting command for raising issues from a session.
- Documentation examples refreshed, and stale test fixtures removed.

**No frontend changes in this release.** The interface is unchanged from `v2.1.4`.

---

**Upgrade note:** no migrations and no configuration changes.
