# ──────────────────────────────────────────────────────────────────────
#  Publish lex-app to PyPI.
#
#  A PyPI release is gated on a single check:
#    showcase_tests.yml — runs the selected tests from
#                         ``lex/test_project/tests/`` with coverage,
#                         emits coverage.xml as a CI artifact, builds
#                         the "Platform Health Report" (HTML + PDF),
#                         and ALWAYS sends the branded email via
#                         SendGrid on EVERY run — pass or fail — so
#                         stakeholders always get a status update on
#                         release day.
#
#  History: ``django_tests.yml`` used to run as a second, parallel
#  gate. On 22 April 2026 it was absorbed into ``showcase_tests.yml``
#  — both workflows were running the same test selector and building
#  the same coverage.xml, so running them separately doubled the CI
#  cost with no added signal.
#
#  If the gate fails, the ``publish`` job is skipped and no version
#  is pushed to PyPI. If it passes, the sdist + wheel are built and
#  uploaded.
#
#  Version is derived from the GitHub release tag (e.g. v2.0.0rc124
#  → 2.0.0rc124). The version in pyproject.toml is dynamic — it reads
#  from lex/_version.py which this workflow writes before building.
#
#  Trigger: `release: released`. This is the PUBLISH half of the
#  release flow. It fires ONLY when a release is published as a full
#  (non-draft, non-pre-release) release.
#
#  The VALIDATE half lives in validate_and_release.yml. It runs this
#  same gate FIRST against a dispatched commit and, ONLY on green, cuts
#  the real release at that exact commit — so a failing gate never
#  mints a tag and the version name is never "spent". The intended
#  operator flow is a single dispatch:
#    1. Dispatch validate_and_release.yml with version vX.Y.ZrcN.
#       It gates the dispatched commit and publishes NOTHING. On green
#       it creates a full release at that commit, which fires
#       `released` → this workflow ships it. If it fails, fix code and
#       re-dispatch the same version — no tag was created.
#
#  Single gate (no redundant re-run): the release that
#  validate_and_release.yml cuts carries a `lex:gate-passed` marker in
#  its body. The gate-release job below SKIPS itself when it sees that
#  marker — the commit was already validated, so re-running the same
#  suite adds no signal and a flaky second run could mint a tag it then
#  refuses to publish. The `publish` job runs when the gate PASSED or
#  was SKIPPED-because-validated. Every OTHER path is still fully gated
#  here: a release cut manually in the UI, or by
#  copilot_publish_after_merge.yml, has no marker, so this gate runs.
#
#  Why `released` and not `created`: `created` fires for pre-releases
#  too, so it could not separate validate from publish. `released`
#  never fires for pre-releases or drafts. Programmatic callers
#  (e.g. copilot_publish_after_merge.yml) that want to publish in one
#  shot create a NON-draft, NON-pre-release release, which fires
#  `released` directly — still a single publish.
#
#  Required secrets:
#   - PYPI_API_TOKEN               (PyPI API token with upload permission)
#   - SENDGRID_API_KEY             (used by the showcase workflow)
#   - SHOWCASE_REPORT_RECIPIENTS
#   - SHOWCASE_REPORT_FROM
#
#  Optional Keycloak integration secrets inherited by showcase_tests.yml:
#   - KEYCLOAK_URL
#   - KEYCLOAK_REALM or KEYCLOAK_REALM_NAME
#   - OIDC_RP_CLIENT_ID
#   - OIDC_RP_CLIENT_SECRET
#   - OIDC_RP_CLIENT_UUID
# ──────────────────────────────────────────────────────────────────────

name: Publish to PyPI

on:
  release:
    # `released` fires only for FULL (non-pre-release, non-draft)
    # releases — either created directly, or by validate_and_release.yml
    # once its gate is green. Pre-releases fire `prereleased` and drafts
    # fire nothing, so neither can trip this publish. See header for how
    # the gate below is skipped for already-validated releases.
    types: [released]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run — build but do not upload to PyPI"
        required: false
        default: "false"
        type: choice
        options:
          - "true"
          - "false"
      version:
        description: "Version to publish (e.g. 2.0.0rc124). Required for manual runs."
        required: false
        type: string
      clusters:
        description: >-
          Cluster selector fed to the release gate.
          Same syntax as ``run_showcase_suite.py --only``. Default runs
          all 13 registered clusters. Change this to trial-run a narrower
          subset from a manual dispatch.
        required: false
        type: string
        default: >-
          init,crud_api,api_layer,calculations,validation_hooks,celery_async,history,audit_logging,permissions,signals_ws,serializers,exports,queries

jobs:
  # ── Step 1: Gate on the selected tests + emit the customer report ─
  # showcase_tests.yml is the ONLY release gate (the former
  # django_tests.yml was absorbed into it on 22 April 2026 — it ran
  # the same tests twice with the same selector, so we consolidated).
  #
  # This job:
  #   * runs the tests (coverage run) for the configured selector,
  #   * emits coverage.xml as a CI artifact ("backend-coverage-report"),
  #   * builds the Platform Health Report HTML + PDF,
  #   * ALWAYS emails the report via SendGrid — pass or fail,
  #   * exits non-zero iff any cluster failed.
  #
  # The `publish` step below will not run unless this gate is green.
  #
  # SKIP CONDITION: validate_and_release.yml already runs this exact gate
  # before it cuts the release, and stamps a `lex:gate-passed` marker into
  # the release notes. When we see that marker we skip the gate here —
  # re-running the same suite on the same commit adds no signal and a flaky
  # second run could mint a tag it then refuses to publish. Every other
  # path (manual release cut in the UI, copilot_publish_after_merge.yml)
  # has NO marker, so it is gated here exactly as before. Manual
  # workflow_dispatch has no release body, so the gate runs too.
  gate-release:
    name: "Release Gate: Tests + Customer Email"
    if: ${{ !contains(github.event.release.body || '', 'lex:gate-passed') }}
    uses: ./.github/workflows/showcase_tests.yml
    secrets: inherit
    with:
      # Single source of truth for the test scope. Edit here (or
      # override from workflow_dispatch) — the same selector drives
      # both the release gate and the email content.
      clusters: ${{ inputs.clusters || github.event.inputs.clusters || 'init,crud_api,api_layer,calculations,validation_hooks,celery_async,history,audit_logging,permissions,signals_ws,serializers,exports,queries' }}
      # Mark this as a release-pipeline run so the email subject can
      # distinguish it from operator-triggered manual dispatches.
      run_kind: release

  # ── Step 2: Build and publish (only if the gate passes) ──────────
  publish:
    name: "Build & Publish"
    needs: [gate-release]
    # Publish when the gate PASSED, or when it was SKIPPED because the
    # release was already validated by validate_and_release.yml (marker
    # present). A gate that actually ran and FAILED (or was cancelled)
    # leaves result != success/skipped → publish is skipped, so a red
    # build still never reaches PyPI.
    if: ${{ always() && (needs.gate-release.result == 'success' || needs.gate-release.result == 'skipped') }}
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install --upgrade pip build twine

      - name: Determine version from release tag
        id: version
        run: |
          if [[ -n "${{ inputs.version }}" ]]; then
            VERSION="${{ inputs.version }}"
          else
            # Read the tag straight from the release payload. On a
            # `released` event the tag is real, but the payload value is
            # authoritative regardless (and works for promoted pre-releases).
            # Strip leading 'v': v2.0.0rc124 → 2.0.0rc124.
            TAG="${{ github.event.release.tag_name }}"
            VERSION="${TAG#v}"
          fi
          if [[ -z "${VERSION}" ]]; then
            echo "::error::Could not determine version (no inputs.version and release.tag_name was empty)."
            exit 1
          fi
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Publishing version: ${VERSION}"

      - name: Write version to lex/_version.py
        run: |
          echo '__version__ = "${{ steps.version.outputs.VERSION }}"' > lex/_version.py
          cat lex/_version.py

      # The frontend is published separately, as lex-app-frontend on PyPI, and is
      # installed here so it ships inside the lex-app wheel exactly as it does
      # today. Which version comes from frontend-version.txt: an exact version,
      # or `latest` (the default).
      #
      # This runs BEFORE the build, because the bundle has to be in the tree
      # for setuptools to package it — see "lex.react" in pyproject.toml.
      - name: Vendor the published frontend
        id: frontend
        run: |
          set -euo pipefail
          VERSION=$(python .github/scripts/vendor_frontend.py --print-version | tail -1)
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"

      # `latest` is not recoverable from a git tag, so the resolved version is
      # recorded where it survives: inside the bundle (and so inside the wheel)
      # by the script above, and on the release itself here. Without this an
      # unpinned release is unattributable six months later, which is the
      # problem the provenance work exists to prevent.
      - name: Record the frontend version on the release
        if: ${{ github.event_name == 'release' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
          FRONTEND: ${{ steps.frontend.outputs.VERSION }}
        run: |
          set -euo pipefail
          BODY=$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json body --jq '.body')
          if printf '%s' "$BODY" | grep -q 'lex:frontend-version'; then
            echo "Already recorded on $TAG — leaving it alone."
            exit 0
          fi
          printf '%s\n\n<!-- lex:frontend-version %s -->\n' "$BODY" "$FRONTEND" > body.md
          gh release edit "$TAG" --repo "$GITHUB_REPOSITORY" --notes-file body.md
          echo "Recorded lex-app-frontend $FRONTEND on $TAG."

      - name: Build sdist and wheel
        run: python -m build

      - name: Check dist with twine
        run: twine check dist/*

      - name: Publish to PyPI
        if: ${{ github.event.inputs.dry_run != 'true' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
